package Abstract;

import Entities.Campaign;

public interface CompaignService {
	
	void addData(Campaign campaign);
	void updateData(Campaign campaign);
	void deleteData(Campaign campaign);
}
